# DB-HA-Manager 后端依赖

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 数据验证
pydantic>=2.5.0
pydantic-settings>=2.1.0

# YAML 配置
pyyaml>=6.0

# SSH 远程执行
paramiko>=3.3.0
asyncssh>=2.14.0

# Oracle 数据库驱动（可选，用于轻量查询）
python-oracledb>=2.1.0

# 加密
cryptography>=41.0.0

# 日志
loguru>=0.7.2

# 任务队列（可选）
# celery>=5.3.0
# redis>=5.0.0

# 数据库
# sqlite3 (内置)

# 开发依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
black>=23.11.0
ruff>=0.1.6
mypy>=1.7.0
