# Agent 接力与收口规则

## 目的
本文件用于规范 ChatGPT、Claude、Codex 在 DB-HA-Manager 项目中的协作方式，降低上下文过长、反复误判、频繁回滚、提示词失焦等问题，提高串行开发效率。

---

## 一、核心分工

### 1. 用户
- 决定是否开启新的 Claude / Codex / ChatGPT 会话
- 将 Claude / Codex 的原始输出逐字粘贴给 ChatGPT
- 手动更新项目文档（当 ChatGPT 判断需要更新时）

### 2. ChatGPT
作为项目总控开发助手，固定负责：
1. 读取用户粘贴的 Claude / Codex 原始输出
2. 判断是否建议为 Claude 或 Codex 开启新会话
3. 判断是否需要更新项目文档
4. 输出下一步给 Claude 或 Codex 的精准提示词
5. 在建议开启新会话时，直接给出需要手动更新的文档内容
6. 尽量把任务组织成“小闭环”，而不是过度碎片化推进
7. 始终以 `doc/step2_latest_requirements_and_design.md` 判断第 2 步的整体完成度
8. 不得把若干轮小范围后端修正误判为“第 2 步整体完成”
9. 只要下一轮提示词依赖更新后的 doc，必须先给出文档更新命令，再给提示词
10. 当用户决定开启新的 GPT 会话时，必须输出新的标准项目开发交接包，并把“阶段性收口 ≠ 整体完成”的约束写入交接包

### 3. Claude
适合：
- 故障定位
- 前端大页面小步精修
- 跨前后端只读审计
- 小范围实施 + 自检

不适合：
- 大范围重构
- 长链路无边界扩改

### 4. Codex
适合：
- 单文件后端规则式修改
- 边界清晰的小实施
- 有明确区块和 diff 预期的任务

不适合：
- 大型 React 页面零碎收口
- 范围含混的大改

---

## 二、项目文档体系

项目文档固定使用以下 3 类：

### 1. `doc/project_working_context.md`
用途：
- 长期稳定背景
- 项目目标
- 技术栈
- 五步走策略
- 冻结规则
- 禁止触碰的主链路
- 已验证环境

更新原则：
- 只有长期背景发生变化时才更新

### 2. `doc/dev_decisions.md`
用途：
- 记录已确认决策
- 防止新会话重新争论旧问题
- 记录禁区、踩坑结论、固定策略

更新原则：
- 只有出现新的“已定决策”时才更新

### 3. `doc/dev_handoff/CURRENT.md`
用途：
- 当前阶段的唯一交接入口
- 给新会话快速接力用
- 记录当前阶段、当前目标、当前阻塞点、下一步

更新原则：
- 只有在判断应该开启新会话时才更新

---

## 三、文档更新规则（最新版本）

### 总规则
默认不更新文档。

### 只有在以下前提成立时才更新文档
- ChatGPT 判断：当前应该开启新的 Claude 或 Codex 会话
- 或者用户明确要求为了新会话交接而更新文档

### 文档更新方式
- 不再让 Claude 或 Codex 负责更新文档
- 由 ChatGPT 直接输出“需要手动更新的文档内容”
- 用户手动将内容写入项目文档
- 若提示词依赖更新后的文档，则必须先给文档更新命令，再给提示词

### 更新优先级
1. 优先更新 `doc/dev_handoff/CURRENT.md`
2. 如有新决策，再更新 `doc/dev_decisions.md`
3. 仅在长期背景变化时，才更新 `doc/project_working_context.md`
4. 若会话切换规则本身发生变化，再更新 `agent_relay_rules.md`

### 文档更新输出格式
当需要更新 doc 下文件时，ChatGPT 必须直接给出完整覆盖命令，不给 patch：
- `cat > /home/lujingwei/DB-HA-Manager/doc/... <<'EOF'`
- `...`
- `EOF`

当需要更新项目根目录下的接力规则文档时，也同样使用完整覆盖命令：
- `cat > /home/lujingwei/DB-HA-Manager/agent_relay_rules.md <<'EOF'`
- `...`
- `EOF`

---

## 四、会话切换判定规则

### A. Claude 何时建议开启新会话
满足任意一条，ChatGPT 应建议开启新的 Claude 会话：
1. 同一轮超过 8~10 轮来回
2. 已出现 2 次以上明显误判
3. 已经历“审计 -> 实施 -> 验收”完整链路
4. 同一文件发生反复大回滚后又重做
5. 任务阶段发生变化（例如：前端收口 -> 故障定位）

### B. Codex 何时建议开启新会话
满足任意一条，ChatGPT 应建议开启新的 Codex 会话：
1. 已完成一个明确小目标
2. 从计划进入实施后又返工
3. 文件范围变化
4. 任务阶段变化（例如：前端 -> 后端）
5. 原任务边界已经变化，不再适合继续复用当前会话
6. 当前阶段已经从“审计”切到“实施”
7. 已完成一个阶段性小闭环，下一轮目标与上一轮不再相同

### C. ChatGPT 自身
- 是否开启 ChatGPT 新会话，由用户决定
- 当用户要求时，ChatGPT 负责输出新的标准交接包，用于开启下一轮 ChatGPT 会话
- 若因为上下文过长开启新 GPT 会话，交接包中必须明确写入：
  - 当前只能算阶段性收口，不是整体完成
  - 仍需持续对照 `doc/step2_latest_requirements_and_design.md`
  - 后续默认按“多轮审计 -> 小闭环修改 -> 验收 -> 必要时前后端最小联调”的节奏推进

---

## 五、提示词生成规则

当用户粘贴 Claude / Codex 原始输出后，ChatGPT 固定做以下判断：

### 1. 是否建议开启新会话
输出：
- Claude：继续当前会话 / 建议开启新会话
- Codex：继续当前会话 / 建议开启新会话

### 2. 是否需要更新文档
输出：
- 是否需要更新 `project_working_context.md`
- 是否需要更新 `dev_decisions.md`
- 是否需要更新 `dev_handoff/CURRENT.md`
- 是否需要更新 `agent_relay_rules.md`

### 3. 下一步由谁做
输出：
- Claude / Codex
- 当前任务类型：审计 / 计划 / 实施 / 验收 / 联调准备

### 4. 文档与提示词顺序
若下一轮提示词依赖更新后的文档，则响应顺序必须固定为：
1. 先输出文档更新命令
2. 再输出可直接执行的提示词

不得先给提示词，再补文档。

### 5. 直接给出可执行提示词
提示词默认包含：
- 需要读取的项目文档
- 当前阶段唯一目标
- 本轮边界
- 允许修改的文件
- 禁止修改的文件
- 是否先出计划
- 是否直接实施
- 改完立即停止

### 6. 当前阶段整体完成度判断
在 DB-HA-Manager 第 2 步相关任务中，ChatGPT 必须始终区分：
- 阶段性小闭环完成
- 第 2 步整体完成

除非已经完成针对 `doc/step2_latest_requirements_and_design.md` 的全量逐项验收，否则不得把：
- 某几个字段修正
- 若干轮后端小修
- 单个模块收口
写成“第 2 步整体完成”。

---

## 六、效率规则：做小闭环，不做小碎步

### 原则
不要默认一次只推进一个极小点。

### 推荐做法
尽量把单轮任务组织成“小闭环”，例如：
- 故障定位 + 最小修复方案
- 小范围实施 + 自检
- 计划输出 + 小实施
- 实施完成 + 验收判断
- 全量差距审计 + 下一轮实施主题确认

### 不推荐做法
避免这种过碎拆分：
- 第 1 轮只确认是不是前端问题
- 第 2 轮只确认是不是 timeout
- 第 3 轮只确认是不是代理
- 第 4 轮才讨论修哪个文件
- 第 5 轮才真正改

### Claude 的单轮理想目标
一次完成以下之一：
- 故障定位闭环
- 前端某一块收口闭环
- 跨前后端只读验收闭环
- 阶段性差距审计闭环

### Codex 的单轮理想目标
一次完成以下之一：
- 后端单文件小闭环
- 计划确认后的小实施闭环
- 实施后自检闭环
- 只读审计后给出下一轮最小实施母稿

---

## 七、Codex 使用规则

### 必须采用“两段式”
#### 第一段：先计划，不改代码
必须写清楚：
- 只允许修改哪些文件
- 明确禁止修改哪些文件
- 精确到代码区块
- 预计改动行数
- 哪些内容不会改

#### 第二段：确认后再实施
必须满足：
- 只改已确认区块
- 控制总 diff
- 改完立即停止
- 不顺手优化

### Codex 优先适用场景
- 后端规则式小改动
- 单文件边界清晰的实施
- 预计 diff 可控的任务
- 基于真实代码的只读审计
- 产出下一轮最小实施母稿

### Codex 避免场景
- 大型 React 页面精修
- 大范围不确定性问题定位
- 需要多轮审计的 UI 细节收口
- 尚未完成真源对照就直接宣称整体完成的任务

---

## 八、当前项目特殊禁区

除非出现新的明确证据，否则不要轻易修改：
- `backend/app/core/executor.py` 中 execute_sql 主链路
- `query_multi_lines`
- sqlplus 主执行方案

当前第 2 步 / 第 3 步开发中：
- 不要顺手修历史类型错误
- 不要大改 schema
- 不要重构整个 SetupWizard 页面
- 不要发散到第 4 步 / 第 5 步

---

## 九、DB-HA-Manager 第 2 步专项约束

### 1. 真源优先级
第 2 步相关判断一律优先以：
- `doc/step2_latest_requirements_and_design.md`
为准。

若旧冻结稿、旧交接说明、旧实现习惯、旧阶段结论与该文档冲突，一律按“旧内容待更新、代码按最新真源修正”处理。

### 2. 整体完成度判断原则
第 2 步不是“改几个字段就算完成”的页面，而是一个三层合一确认页：
1. 字段最终展示值
2. 字段来源标签
3. 结构化预检查结论

因此：
- 某几轮后端小修完成 != 第 2 步整体完成
- 某一轮前端联动完成 != 第 2 步整体完成
- 某个模块通过验收 != 第 2 步整体完成

只有当主库字段、备库字段、配置摘要/环境摘要、预检查规则、预检查提示模块都经过真源对照与逐项验收后，才允许判断“第 2 步整体完成”。

### 3. 默认推进节奏
后续默认按以下顺序推进：
1. 多轮审计
2. 小闭环修改
3. 验收
4. 必要时前后端最小联调

不得跳过审计和验收，直接把小范围后端修正写成整体完成。

### 4. 新 GPT 会话交接要求
若用户因为上下文过长要求开启新的 GPT 会话，新的交接包里必须显式写入：
- 当前第 2 步仍是阶段性收口中
- 不能把已完成的小闭环误判为整体完成
- 后续必须继续对照 `doc/step2_latest_requirements_and_design.md`
- 后续仍按“多轮审计 -> 小闭环修改 -> 验收 -> 必要时前后端最小联调”的节奏推进

---

## 十、ChatGPT 的固定响应格式

当用户粘贴 Claude / Codex 原始输出后，优先按以下格式响应：

### A. 是否建议开启新会话
- Claude：继续 / 建议新会话
- Codex：继续 / 建议新会话

### B. 是否需要更新项目文档
- 需要更新哪些文档
- 为什么需要更新
- 如果需要开新会话，先给出手动更新命令

### C. 下一步建议由谁做
- Claude / Codex
- 当前任务类型：审计 / 计划 / 实施 / 验收 / 联调准备

### D. 可直接使用的下一条提示词
- 只有在先给出文档更新命令之后，才给完整可执行提示词

### E. 本轮目标是什么闭环
- 明确这轮要完成哪个“小闭环”
- 同时说明这是否只是阶段性收口，还是整体完成判断的一部分

---

## 十一、默认执行原则

1. 优先保持边界清晰
2. 优先做小闭环
3. 优先减少无意义审计轮次
4. 非必要不更新文档
5. 只有判断应开启新会话时才更新文档
6. 文档更新内容由 ChatGPT 直接输出，用户手动写入
7. Claude / Codex 不再负责文档维护
8. ChatGPT 负责跨轮次协作控制与提示词生成
9. 在第 2 步相关任务中，优先区分“阶段性收口”与“整体完成”
10. 任何时候若存在歧义，优先回到 `doc/step2_latest_requirements_and_design.md` 对照整体差距

