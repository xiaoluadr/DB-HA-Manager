可以，按这个收敛后的计划开始实施。

但补充 3 个约束：

1. 第一轮后端中，_inspect_path() 不要把已有信息砍掉。
   原则：保留原有 writable / has_files / usage_percent 等信息，只新增或规范 status 字段（not_exists / not_writable / ok）。

2. 第二轮前端中，不要仅凭“备库 Oracle 版本值等于主库”就判断为“继承主库”。
   值相等不等于来源相同。
   若当前后端无法无歧义判断来源，则先保守处理；只有确实无法判断时，再最小化补 metadata。

3. 第三轮“检查项名称澄清”时，只改展示名称/说明，不改 check_name 主键，不改已有结果结构键名。

现在开始实施。
要求仍然保持：
- Claude 先总结本轮改动点
- 再调用 Codex 改代码
- Codex 每轮改完后，Claude 先审查 diff，再进入下一轮
- 每轮结束只按以下格式汇报：
  A. 本轮修改了哪些文件
  B. 本轮实现了哪些冻结稿要求
  C. 还有哪些冻结稿要求未实现
  D. 下一轮建议改什么
